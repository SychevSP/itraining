import {StyleSheet} from 'react-native';

import { TITLE_FONT_SIZE } from 'Components/consts';

export default styles = StyleSheet.create({
    input: {
        width: '85%',
        height: TITLE_FONT_SIZE + TITLE_FONT_SIZE / 2,
        color: 'white',
        borderColor: 'gray',
        borderWidth: 2,
        borderRadius: TITLE_FONT_SIZE / 2,
    },
});
